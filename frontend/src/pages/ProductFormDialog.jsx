import { useEffect, useState } from 'react';
import { ImagePlus, X } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
import styles from './AdminProductsPage.module.css';
import modalStyles from './AdminProductsModal.module.css';
import formStyles from './ProductFormDialog.module.css';

const empty = { name: '', sku: '', categoryName: '', brandName: '', basePrice: '', compareAtPrice: '', offerStartsAt: '', offerEndsAt: '', physicalQuantity: 0, reorderPoint: 0, weightGrams: '', lengthCm: '', widthCm: '', heightCm: '', shortDescription: '', description: '', imageUrls: [], specifications: {}, status: 'ACTIVE', isFeatured: false };
const dateValue = (value) => value ? new Date(value).toISOString().slice(0, 16) : '';
const toLines = (specifications) => Object.entries(specifications || {}).map(([key, value]) => `${key}: ${value}`).join('\n');
const parseLines = (value) => Object.fromEntries(value.split('\n').map((line) => line.split(/:(.*)/)).filter(([key, detail]) => key?.trim() && detail?.trim()).map(([key, detail]) => [key.trim(), detail.trim()]));
const nullableNumber = (value) => value === '' || value == null ? null : Number(value);

export function ProductFormDialog({ product, onCancel, onSaved, onFailed }) {
  const { request } = useAuth();
  const [values, setValues] = useState(() => ({ ...empty, ...product, categoryName: product?.category || '', brandName: product?.brand || '', offerStartsAt: dateValue(product?.offerStartsAt), offerEndsAt: dateValue(product?.offerEndsAt) }));
  const [imageUrlsText, setImageUrlsText] = useState(() => (product?.imageUrls || (product?.imageUrl ? [product.imageUrl] : [])).join('\n'));
  const [specificationText, setSpecificationText] = useState(() => toLines(product?.specifications));
  const [error, setError] = useState('');
  const [saving, setSaving] = useState(false);
  useEffect(() => { const previous = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = previous; }; }, []);
  const update = (key, value) => setValues((current) => ({ ...current, [key]: value }));
  const submit = async (event) => {
    event.preventDefault(); setError('');
    if (!values.categoryName.trim()) return setError('Indica una categoría.');
    setSaving(true);
    try {
      const payload = { ...values, categoryName: values.categoryName.trim(), brandName: values.brandName.trim(), basePrice: Number(values.basePrice), compareAtPrice: nullableNumber(values.compareAtPrice), physicalQuantity: Number(values.physicalQuantity), reorderPoint: Number(values.reorderPoint), weightGrams: nullableNumber(values.weightGrams), lengthCm: nullableNumber(values.lengthCm), widthCm: nullableNumber(values.widthCm), heightCm: nullableNumber(values.heightCm), offerStartsAt: values.offerStartsAt ? new Date(values.offerStartsAt).toISOString() : null, offerEndsAt: values.offerEndsAt ? new Date(values.offerEndsAt).toISOString() : null, imageUrls: imageUrlsText.split('\n').map((url) => url.trim()).filter(Boolean), specifications: parseLines(specificationText) };
      const data = await request(values.id ? `/admin/products/${values.id}` : '/admin/products', { method: values.id ? 'PUT' : 'POST', body: JSON.stringify(payload) });
      onSaved(data.product, values.id ? 'updated' : 'created');
    } catch (requestError) { setError(requestError.message); onFailed?.(requestError.message); } finally { setSaving(false); }
  };
  return <div className={`${styles.backdrop} ${modalStyles.backdrop}`} role="presentation"><section className={`${styles.dialog} ${modalStyles.dialog} ${formStyles.dialog}`} role="dialog" aria-modal="true" aria-labelledby="product-form-title"><header><div><p>Administración de catálogo</p><h2 id="product-form-title">{values.id ? 'Editar producto' : 'Nuevo producto'}</h2></div><button type="button" aria-label="Cerrar" onClick={onCancel}><X /></button></header><form onSubmit={submit}>{error && <p className={styles.error}>{error}</p>}<section className={formStyles.section}><h3>Información básica</h3><div className={styles.formGrid}><label>Nombre<input required value={values.name} onChange={(event) => update('name', event.target.value)} /></label><label>SKU<input required value={values.sku} onChange={(event) => update('sku', event.target.value)} placeholder="Ej. FEF-LAP-001" /></label><label>Categoría<input required value={values.categoryName} onChange={(event) => update('categoryName', event.target.value)} placeholder="Ej. Computadores" /></label><label>Marca<input value={values.brandName} onChange={(event) => update('brandName', event.target.value)} placeholder="Ej. Lenovo" /></label><label className={styles.full}>Descripción corta<textarea maxLength="500" value={values.shortDescription || ''} onChange={(event) => update('shortDescription', event.target.value)} /></label><label className={styles.full}>Descripción completa<textarea rows="4" value={values.description || ''} onChange={(event) => update('description', event.target.value)} /></label></div></section><section className={formStyles.section}><h3>Precio y oferta</h3><p>Para una oferta por tiempo limitado, completa el precio anterior y ambas fechas.</p><div className={styles.formGrid}><label>Precio de venta<input type="number" min="0" required value={values.basePrice} onChange={(event) => update('basePrice', event.target.value)} /></label><label>Precio anterior<input type="number" min="0" value={values.compareAtPrice ?? ''} onChange={(event) => update('compareAtPrice', event.target.value)} /></label><label>Inicio de oferta<input type="datetime-local" value={values.offerStartsAt} onChange={(event) => update('offerStartsAt', event.target.value)} /></label><label>Fin de oferta<input type="datetime-local" value={values.offerEndsAt} onChange={(event) => update('offerEndsAt', event.target.value)} /></label></div></section><section className={formStyles.section}><h3>Inventario y envío</h3><div className={styles.formGrid}><label>Inventario físico<input type="number" min="0" required value={values.physicalQuantity} onChange={(event) => update('physicalQuantity', event.target.value)} /></label><label>Punto de reposición<input type="number" min="0" required value={values.reorderPoint} onChange={(event) => update('reorderPoint', event.target.value)} /></label><label>Peso (gramos)<input type="number" min="0" value={values.weightGrams ?? ''} onChange={(event) => update('weightGrams', event.target.value)} /></label><label>Largo (cm)<input type="number" min="0" value={values.lengthCm ?? ''} onChange={(event) => update('lengthCm', event.target.value)} /></label><label>Ancho (cm)<input type="number" min="0" value={values.widthCm ?? ''} onChange={(event) => update('widthCm', event.target.value)} /></label><label>Alto (cm)<input type="number" min="0" value={values.heightCm ?? ''} onChange={(event) => update('heightCm', event.target.value)} /></label></div></section><section className={formStyles.section}><h3>Imágenes y especificaciones</h3><div className={styles.formGrid}><label className={styles.full}>URLs de imágenes<textarea rows="3" value={imageUrlsText} onChange={(event) => setImageUrlsText(event.target.value)} placeholder={'Una URL por línea\nhttps://...'} /><small><ImagePlus size={15} /> La primera imagen será la principal.</small></label><label className={styles.full}>Especificaciones técnicas<textarea rows="4" value={specificationText} onChange={(event) => setSpecificationText(event.target.value)} placeholder={'Procesador: Intel Core i5\nMemoria RAM: 16 GB\nAlmacenamiento: 512 GB SSD'} /><small>Una especificación por línea, con formato “Nombre: valor”.</small></label></div></section><section className={formStyles.section}><h3>Visibilidad</h3><div className={formStyles.visibility}><label>Estado<select value={values.status} onChange={(event) => update('status', event.target.value)}><option value="ACTIVE">Activo</option><option value="DRAFT">Borrador</option><option value="INACTIVE">Inactivo</option></select></label><label className={formStyles.featured}><input type="checkbox" checked={Boolean(values.isFeatured)} onChange={(event) => update('isFeatured', event.target.checked)} /><span><b>Mostrar en productos destacados</b><small>Se verá en la sección principal de la tienda.</small></span></label></div></section><footer><button type="button" className={styles.secondary} onClick={onCancel}>Cancelar</button><button disabled={saving}>{saving ? 'Guardando…' : 'Guardar producto'}</button></footer></form></section></div>;
}
